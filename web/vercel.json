{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd .. && corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install --config.engine-strict=false",
  "buildCommand": "cd .. && pnpm --filter web-nuxt-ui... run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
